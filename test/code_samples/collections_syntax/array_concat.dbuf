message T {
  array1 Array (Int) 1u;
  array2 Array (Int) 2u;
  array3 Array (Int) 3u;
  array4 Array (Int) 4u;
}

message K (array Array (Int) 10u) {
}

message P (t T) {
  k K (t.array1 || t.array2 || t.array3 || t.array4);
}
