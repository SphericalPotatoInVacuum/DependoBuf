enum Nat {
  Zero {}
  Succ {
    prev Nat;
  }
}

enum IntList (size Nat) {
  Zero {} => {
    Nil {}
  }
  
  Succ {prev: tailSize} => {
    IntList {
      head Int;
      tail IntList tailSize;
    }
  }
}